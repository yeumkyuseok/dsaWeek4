package dsaWeek4;

public class main {
	public static void main(String[] args) {
		
		/*Linked List
		DSALinkedList L1 = new DSALinkedList();
		
		L1.insertLast("one");
		L1.insertLast("two");
		L1.insertLast("three");
	
		Iterator it = L1.iterator();
		
		while(it.hasNext()) {
			System.out.println(it.next());
		}
		//*/
		
		
		
		/*stack using LinkedList
		DSAStack s1 = new DSAStack();
		s1.push("one");
		s1.push("two");
		s1.push("three");
		s1.pop();
		s1.push("four");
		
		Iterator it = s1.stack.iterator();
		while(it.hasNext()) {
			System.out.println(it.next());
		}
		//*/
		
		/*
		DSAQueue q1 = new DSAQueue();
		q1.enqueue("one");
		q1.enqueue("two");
		q1.enqueue("three");
		q1.dequeue();
		q1.enqueue("four");
		
		Iterator it = q1.queue.iterator();
		while(it.hasNext()) {
			System.out.println(it.next());
		}
		//*/
	}
}
